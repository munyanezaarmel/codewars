function rotation(arr) {
    const newPosition = []
    const second=[]
    for (let i = 0; i < arr.length; i++){
        const index = i * 4
        newPosition.push(index)
    }
    for (let i = 0; i<newPosition.length;i++){
        
    }
    return newPosition
}
console.log(rotation([12, 4, 5, 6, 566, 67]));